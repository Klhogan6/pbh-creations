// Landing page — sections are composed here from components/marketing/
// Each section is built on its own feature branch and imported here.
export default function HomePage() {
  return (
    <main>
      {/* Nav — feature/nav-hero */}
      {/* Hero — feature/nav-hero */}
      {/* Problem/Solution — feature/problem-how-it-works */}
      {/* How It Works — feature/problem-how-it-works */}
      {/* Pricing — feature/pricing */}
      {/* Why PBH — feature/why-contact-footer */}
      {/* Contact — feature/why-contact-footer */}
      {/* Footer — feature/why-contact-footer */}
      <div className="flex min-h-screen items-center justify-center">
        <p className="text-gray-400 text-sm">PBH Creations — scaffold</p>
      </div>
    </main>
  );
}
